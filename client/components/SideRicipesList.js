import React from "react";
import { View, Text } from "react-native";

const SideRecipesList = ({ navigation }) => {
  return (
    <View>
      <Text>side</Text>
    </View>
  );
};

export default SideRecipesList;
